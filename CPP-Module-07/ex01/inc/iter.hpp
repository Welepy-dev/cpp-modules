/*
template <typename T>
T add(T a, T b) {
    return a + b;
}
*/

//int (*fp)(int, int);   // fp is a pointer to a function taking (int,int) and returning int


template <typename T>
void	iter(T *a, const T size, void(*f)(T))
{
	
}